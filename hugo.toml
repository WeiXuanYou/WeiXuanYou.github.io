baseURL = "https://weixuanyou.github.io/"
languageCode = "en-us"
title = "WeiXuan's R&D Lab"
theme = "PaperMod"

# --- 核心設定修正 (Fix for Hugo v0.128+) ---
[pagination]
    pagerSize = 10          # 每頁顯示幾篇文章 (取代舊的 paginate)

enableRobotsTXT = true      # 允許搜尋引擎爬取
buildDrafts = false         # 不發布草稿
buildFuture = false
buildExpired = false

# --- 搜尋功能 (Search) ---
[outputs]
    home = ["HTML", "RSS", "JSON"] # 必須開啟 JSON 才能搜尋

[params]
    env = "production"
    description = "WeiXuan's Personal R&D Lab - EDA Optimization, AI Algorithms, and FPGA Acceleration."
    author = "WeiXuan You"
    defaultTheme = "auto"   # 跟隨系統切換深色/淺色模式
    
    # 介面細節設定
    showReadingTime = true  # 技術文章顯示閱讀時間
    showShareButtons = true # 顯示分享按鈕
    showPostDetails = true  # 顯示作者、日期
    showToc = true          # 在文章內顯示目錄 (Table of Contents)
    
    # 搜尋按鈕設定
    [params.fuseOpts]
        isCaseSensitive = false
        score = 0.5
        minMatchCharLength = 2
        keys = ["title", "content", "tags", "categories"]

    # --- 1. 首頁 Hero Section ---
    [params.profileMode]
        enabled = false

    [params.homeInfoParams]
        Title = "WeiXuan's R&D Lab"
        Content = """
        **Principal Investigator: WeiXuan You (游煒亘)**
        
        歡迎來到我的個人研發實驗室。
        這裡致力於將 **AI 演算法 (RL/LLM)** 與 **EDA/FPGA 硬體加速** 結合，探索效能優化的極限。
        
        > **Current Focus:**
        > * ⚡ **EDA Optimization**: Power/Ground Routing Algorithms.
        > * 🤖 **Edge AI**: FPGA Deployment (Vitis) & Model Quantization.
        > * 🧪 **Lab Status**: 🟢 Online | Conducting Experiments...
        """

    # --- 2. 社群連結 ---
    [[params.socialIcons]]
        name = "github"
        url = "https://github.com/WeiXuanYou"
    [[params.socialIcons]]
        name = "linkedin"
        url = "https://linkedin.com/in/your-id" 
    [[params.socialIcons]]
        name = "email"
        url = "mailto:[email protected]"
    [[params.socialIcons]]
        name = "cv"
        url = "/files/resume.pdf"

    # --- 3. 圖片顯示設定 ---
    [params.cover]
        hidden = false
        hiddenInList = false
        hiddenInSingle = false
        responsiveImages = true

# --- 4. 選單架構 ---
[menu]
    # 1. 職涯 Experience (工作列表放在最顯眼的位置)
    [[menu.main]]
        identifier = "experience"
        name = "🚀 Experience"
        url = "/career/"
        weight = 10  # 排第一

    # 2. 研究 Research (你的 Nx2 專案卡片)
    [[menu.main]]
        identifier = "projects"
        name = "🔬 Research"
        url = "/projects/"
        weight = 20  # 排第二

    # 3. 實驗筆記 (Blog)
    [[menu.main]]
        identifier = "lab-notes"
        name = "🧪 Lab Notes"
        url = "/posts/"
        weight = 30

    # 4. 每日紀錄
    [[menu.main]]
        identifier = "daily"
        name = "📅 Daily"
        url = "/daily/"
        weight = 40

    # 5. CV 下載
    [[menu.main]]
        identifier = "cv"
        name = "📄 CV"
        url = "/files/resume.pdf"
        weight = 50